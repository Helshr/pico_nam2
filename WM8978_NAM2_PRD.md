# WM8978 + Pico 2 W + NAM2 实时效果器 PRD

## 1. 目标

在 Raspberry Pi Pico 2 W 上运行 NAM2 A2-Lite 模型，实现：

```text
电吉他 → WM8978 ADC → Pico 双核 NAM2 → WM8978 DAC → 耳机/音箱
```

第一阶段只追求稳定、低噪声、可验证的实时音频链路；WebUSB 动态换音色和多音色存储放到后续阶段。

## 2. 已知基线

- 旧板：Waveshare WM8960 Audio Board。
- 旧版代码已经验证过 I2S 双工、48 帧分块和双核 NAM2 调用时序。
- 旧板能听到吉他/NAM，但模拟噪声较大。
- 新板：WM8978 模块，无板载麦克风路径，使用线路输入。
- 新板旁路直通已经验证能听到干净吉他。
- 当前问题只在 NAM 接入 WM8978 后出现蜂鸣/劈啪，因此本项目不再沿用当前连续环形 DMA 方案。

## 3. 硬件连接

| WM8978 | Pico 2 W | 用途 |
|---|---|---|
| SDA | GP4 | I2C 配置 |
| SCL | GP5 | I2C 配置 |
| SCK/BCLK | GP10 | I2S 位时钟 |
| FSA/LRCLK | GP11 | I2S 左右声道时钟 |
| DAT | GP12 | Pico → WM8978 DAC 数据 |
| SDB | GP13 | WM8978 ADC → Pico 数据 |
| MCK | GP14 | WM8978 主时钟 |
| 3.3 | 3V3 OUT | 逻辑电源 |
| 5V | VBUS | WM8978 模拟/功放电源 |
| GND | GND | 共地 |

输入使用 WM8978 的 LINE IN，输出使用耳机口。暂不使用麦克风路径。

## 4. 软件方案

### 4.1 复用旧方案

以 `src/wm8960_test.c` 中已经验证的阻塞式 I2S 双工收发为模板，复用：

- PIO I2S 时序；
- 48 kHz、16-bit、立体声帧格式；
- 读取左声道并复制到左右输出；
- 分块调用 `src/nam_fx.cpp` 的双核 NAM2 引擎；
- BOOTSEL 旁路/NAM 切换。

### 4.2 WM8978 专属部分

重新实现并单独验证：

- WM8978 I2C 寄存器初始化；
- LINE IN L2/R2 → ADC 路由；
- DAC → 耳机混音器路由；
- ADC/DAC 数字音量、输入增益和输出增益；
- MCLK、BCLK、LRCLK 极性和 I2S 格式。

### 4.3 缓冲策略

使用固定大小的双缓冲/分块队列，不使用当前连续 TX 环形 DMA：

```text
RX block A → NAM block A → TX block A
RX block B → NAM block B → TX block B
```

每个块固定 48 帧，只有完整块处理完成后才允许送入 DAC，避免 TX DMA 读取半更新数据。

## 5. 功能需求

### P0：必须实现

- WM8978 上电并通过 I2C 应答。
- 旁路模式能稳定听到干净吉他。
- NAM 模式能听到模型处理后的吉他。
- NAM/旁路切换不产生持续蜂鸣或锁死。
- 连续播放 5 分钟无明显劈啪、卡顿或死机。

### P1：本阶段完成后实现

- 使用 HBE Mammoth NAM2 作为默认模型。
- 输入无信号时不出现持续自激噪声。
- 左右耳音量基本一致。
- 记录当前可复用的 UF2 和接线说明。

### P2：后续阶段

- WebUSB 导入 NAM2。
- Flash 存储 3–5 个模型。
- 按钮短按切换模型，长按切换旁路。
- 网页显示当前模型和设备状态。

## 6. 测试顺序

1. I2C：确认 WM8978 地址 `0x1A` 应答。
2. MCLK：确认 WM8978 有稳定主时钟。
3. 数字静音：DAC 输出应安静，无蜂鸣。
4. I2S 旁路：吉他输入直通耳机。
5. NAM 静音输入：确认模型空状态不会产生持续音调。
6. NAM + 低电平测试音：确认输出波形连续。
7. NAM + 电吉他：确认有明显音色变化且无劈啪。
8. BOOTSEL：反复切换 20 次，确认不死机、不锁 USB。

## 7. 验收标准

- 旁路和 NAM 两种模式都能实时出声。
- NAM 模式不再出现持续蜂鸣。
- 拨弦起音、延音和停止均连续，没有周期性劈啪。
- 不需要更换音色来掩盖问题。
- 最终固件、源代码、模型文件和烧录步骤均落盘。

## 8. 风险与对策

| 风险 | 对策 |
|---|---|
| WM8978 寄存器路由错误 | 先运行旁路/静音测试，再接 NAM |
| NAM 计算来不及 | 保留双核 `a2_fast`，每块 48 帧 |
| DMA 与 CPU 竞争 | 使用固定块状态机，不共享正在发送的缓冲 |
| 高增益模型放大模拟噪声 | 先保证数字链路正确，再单独调输入增益和噪声门 |
| BOOTSEL 读取影响 Core1 | 沿用旧版 multicore lockout 保护 |

## 9. 交付物

- `src/wm8978_nam.c`：基于旧方案重写的 WM8978 音频适配。
- `firmware/WM8978_NAM2_*.UF2`：每个可验证阶段保留一个版本。
- `firmware/README.md`：烧录、接线和回退方法。
- 本 PRD 及测试记录。
